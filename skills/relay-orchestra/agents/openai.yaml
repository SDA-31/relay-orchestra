interface:
  display_name: "Relay Orchestra"
  short_description: "Coordinate parallel agents, worktrees, and reviewers safely"
  default_prompt: "Enable $relay-orchestra Auto for suitable work later in this chat. Explain how it routes bounded and risky work, then ask what I want to do."
policy:
  allow_implicit_invocation: true
